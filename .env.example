# MongoDB Atlas connection string
# Required: Set this in Render's Environment Variables dashboard
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/"

# Node environment
NODE_ENV="production"
